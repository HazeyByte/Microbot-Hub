# Queryable API Rules

## Best Practices:
- Always use the Queryable API to interact with the game client.
- Ensure that API calls are thread-safe and do not block the main thread.
- Validate all inputs and outputs to prevent errors and unexpected behavior.
- Use consistent naming conventions for API methods and parameters.
```
